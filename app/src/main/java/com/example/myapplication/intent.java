package com.example.myapplication;

import android.content.Intent;

class intent extends Intent {
    public <button> intent(MainActivity<button> buttonMainActivity, Class<RegisterActivity> registerActivityClass) {
    }
}
